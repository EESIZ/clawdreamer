# Required: OpenAI API key (for embeddings)
OPENAI_API_KEY=sk-...

# Optional: MiniMax API key (if using MiniMax as LLM provider)
MINIMAX_API_KEY=

# LLM provider: "openai" (default) or "minimax"
DREAMER_LLM_PROVIDER=openai

# Data directory (default: ~/.dreamer)
DREAMER_HOME=
